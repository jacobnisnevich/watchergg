require 'spec_helper'

describe Watchergg do
  it 'has a version number' do
    expect(Watchergg::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
